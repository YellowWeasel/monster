<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.erayic.agr.index">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.impl.LoginActivity" />
        <activity
            android:name=".view.impl.CodeActivity"
            android:theme="@style/AppTheme.Transparent" />
        <activity
            android:name=".view.impl.RegisterActivity"
            android:theme="@style/AppTheme.Transparent" />
        <activity
            android:name=".view.impl.RegisterByEntActivity"
            android:theme="@style/AppTheme.Transparent" />
        <activity
            android:name=".view.impl.RegisterByUserActivity"
            android:theme="@style/AppTheme.Transparent" />
    </application>

</manifest>
